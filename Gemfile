source "https://rubygems.org"

# Matches the gem set that GitHub Pages runs, so a local preview behaves like
# the deployed site. Only needed if you want to preview locally.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.0+ no longer ships webrick, which `jekyll serve` needs.
gem "webrick", "~> 1.8"
